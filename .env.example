# Example environment variables for Service Helpdesk CRM
# Copy this file to .env and update with your actual values

# ==========================================
# BACKEND ENVIRONMENT VARIABLES
# Location: backend/.env
# ==========================================

# Server Configuration
PORT=5001
NODE_ENV=development

# Database Configuration
# Local MongoDB
MONGODB_URI=mongodb://localhost:27017/proxima_crm

# MongoDB Atlas (Cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/proxima_crm?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=change-this-to-a-very-secure-random-string-in-production
JWT_EXPIRES_IN=1h

# ==========================================
# FRONTEND ENVIRONMENT VARIABLES
# Location: frontend/.env
# ==========================================

# API Configuration
REACT_APP_API_URL=http://localhost:5001/api

# Production API URL (for deployment)
# REACT_APP_API_URL=https://your-backend-domain.com/api

# ==========================================
# SECURITY NOTES
# ==========================================
# 
# 1. NEVER commit .env files to version control
# 2. Use strong, random strings for JWT_SECRET
# 3. Use different secrets for development and production
# 4. Rotate secrets periodically
# 5. Keep JWT_EXPIRES_IN reasonable (1h-7d)
# 6. Use HTTPS in production
# 7. Enable MongoDB authentication in production
# 8. Use environment-specific configuration files
#
# Generate secure JWT secret:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
